# Keel（Spec-Driven Development）规格驱动全栈开发范式与完整流程

&gt; **文档版本**：v1.8
&gt; **最后更新**：2026-07-17
&gt; **适用范围**：全栈个人开发者、小型产研团队
&gt; **核心理念**：规格优先、文档代码共生、全链路闭环、AI友好、反馈驱动

---

## 范式演进：Vibe → Spec → Harness

Keel经历了三个阶段的范式演进，理解演进路径有助于落地：

| 阶段 | 特征 | 问题 |
|------|------|------|
| **Vibe Coding（感觉驱动）** | 需求模糊、边想边写、口头对齐 | AI输出不稳定、反复返工、最后一公里漫长 |
| **Spec Coding（规格驱动）** | 目标明确、规则统一、文档代码共生 | 代码写完到交付仍有质量鸿沟，缺乏自动纠偏 |
| **Harness Engineering（闭环工程）** | 在Spec基础上接入验证反馈回路，AI自动感知Lint/Test/Log/DB状态并自我修正 | 闭环断裂点在「反馈」——AI看不到验证结果就无法自我纠偏 |

Keel在Spec基础上构建Harness闭环：让AI在「目标-状态-执行-反馈」四象限中持续推进任务，人类只在需要工程判断的关键节点介入。

---

## 一、Keel 核心开发范式

Keel 的本质是**契约先行**——所有开发行为都以明确的规格文档为唯一依据，拒绝边想边写、口头对齐。规格不是一次性写完的静态文档，而是分层渐进、与代码同步迭代的活契约。Debug的本质不是修代码，而是**补Spec**——每次bug都说明某条需求/约束/边界没有被说清楚，修复后必须把规则沉淀回规格中。

### 1.1 Keel 分层结构

按「全局→模块→文件」三级拆分，既保证顶层约束不变，又支持底层灵活迭代，同时天然适配AI渐进式读取。

| 层级 | 文档载体 | 核心内容 | 可修改性 |
|------|----------|----------|----------|
| L0 项目总纲 | 根目录 `PROJECT_SPEC.md` | 项目目标、技术栈选型、整体架构图、全局约束（命名规范、安全规则、架构决策ADR）、变更流程 | 禁止随意修改，变更需正式评审 |
| L1 模块规格 | 每个模块目录下 `MODULE_SPEC.md` | 模块职责边界、对外依赖、输入输出、核心能力清单、关联接口 | 谨慎修改，变更需同步影响评估 |
| L2 文件/接口规格 | 核心文件头部注释 / `API_SPEC.md` | 函数/类契约、入参出参、异常处理、字段含义、错误码 | 可随功能迭代修改，必须与代码同步 |

### 1.2 Keel 核心原则

- **无规格不开发**：任何代码修改必须先有对应规格定义，禁止先写代码再补文档。
- **谁变更谁同步**：代码变更必须同步更新对应层级的规格文档，反之亦然。
- **不变量前置**：L0层级的架构决策、技术选型、全局规则属于「不可变边界」，提前明确，避免后期反复推翻重构。
- **Debug即补Spec**：调试不是修bug，是发现「哪些需求没被说清楚」，修复后必须把规则沉淀回Spec。
- **反馈闭环**：代码修改后必须通过Lint/Test/Typecheck/Build验证，让AI能感知结果并自我修正，拒绝「写完就交」。
- **小步快跑**：短分支+小PR+高频合入，一个MDU一个PR，降低AI犯错概率和review成本。
- **Code First写文档**：代码本身能表达的不要重复写进文档（如类型定义、函数签名），文档只写代码推断不出的信息（业务规则、边界约束、决策背景）。

---

## 二、产研测运一体化全流程

将产品、开发、测试、运维的职责嵌入每个迭代环节，而非瀑布式交接，实现全链路左移。

### 阶段1：需求准入与规格输出

**输入**：业务需求/功能构想
**责任人**：产品牵头，开发、测试、运维共同评审
**输出物**：L0/L1级Keel初稿、验收标准、运维要求

1. 产品输出需求背景、用户场景、功能清单、验收标准。
2. 开发输出技术方案、模块拆分、接口定义、工作量评估。
3. 测试输出测试范围、测试用例初稿、风险点。
4. 运维输出部署要求、资源评估、可观测性要求、监控指标。
5. 四方评审通过后，规格正式冻结，进入开发阶段。

### 阶段2：并发开发与单测

**输入**：冻结的Keel规格
**责任人**：前后端开发、测试
**输出物**：特性分支代码、单元测试、模块级文档

- 前后端基于接口契约并行开发。
- 测试同步编写自动化测试用例，而非等开发完成再写。
- 开发过程中若需变更规格，必须走「规格变更流程」：同步更新文档→通知所有相关方→评审通过后再改代码。

### 阶段3：联调与集成测试

**输入**：前后端各自单测通过的代码
**责任人**：前后端开发、测试
**输出物**：集成测试报告、缺陷记录

1. 前后端基于真实接口联调，问题实时录入错误库。
2. 测试执行集成测试、回归测试，不通过则打回开发阶段。
3. 同步更新运维部署文档、配置清单。

### 阶段4：部署上线与验证

**输入**：测试通过的代码包
**责任人**：运维、开发、测试
**输出物**：线上可用版本、监控告警配置

1. 运维按部署规格发布到生产环境，执行灰度/全量发布。
2. 测试在线上环境做冒烟验证，产品做业务验收。
3. 监控指标、日志、告警全部生效。

### 阶段5：运维监控与复盘沉淀

**输入**：线上运行数据、用户反馈
**责任人**：运维、开发、产品
**输出物**：运行报告、错误沉淀、规范更新

1. 运维持续监控可用性、性能、错误率，异常及时告警。
2. 线上问题按故障等级处理，修复后同步更新错误库。
3. 每个迭代结束做复盘：更新Keel规范、补充Checklist、沉淀通用解决方案。

---

## 三、前后端分离并发开发机制

核心是**接口契约解耦**，用Keel中的API规格作为唯一对齐标准，前后端完全独立开发、独立测试，最终无缝联调。

### 3.1 前置：接口契约先行

在L1级Keel中输出完整的`API_SPEC.md`，推荐采用OpenAPI(Swagger)规范，明确定义：

- 接口路径、请求方法、鉴权方式
- 请求参数（Header、Query、Body）的字段名、类型、是否必填、取值范围
- 响应结构：成功返回、错误码、异常场景返回
- 数据格式、时间戳、枚举值等全局约定

契约一经评审通过，双方不得随意修改；确需修改必须走变更流程，同步更新文档并通知对方。

### 3.2 并行开发流

**后端侧**：
1. 先基于契约生成接口Mock（或直接写Mock接口），提供给前端。
2. 按模块实现业务逻辑、数据层、鉴权逻辑。
3. 写完单测，自行用Postman/接口工具验证契约一致性。

**前端侧**：
1. 直接对接Mock接口开发页面、交互、状态管理。
2. 编写组件单测、页面交互测试。
3. Mock数据完全贴合契约定义，禁止自行增减字段。

### 3.3 同步与联调规则

- 每日15分钟轻量同步：同步进度、接口疑问，不做需求变更讨论。
- 联调门禁：前后端各自单测通过率≥90%、契约校验通过，方可进入联调。
- 联调问题分类处理：契约不一致→更新契约；逻辑BUG→按缺陷流程处理。

---

## 四、代码-文档双向绑定机制

从流程、工具、校验三个维度确保「改代码必改文档，改文档必影响代码」，杜绝文档和代码脱节。

### 4.1 存储与组织绑定

- 文档与代码**同仓共存**：所有Keel文档放在项目仓库内，跟随代码版本迭代。
  - 根目录放L0级全局文档
  - 每个模块目录内放对应的`MODULE_SPEC.md`，与代码文件同目录
- 接口文档与代码绑定：推荐文档优先——先定义`API_SPEC.md`，由工具生成前后端类型桩/接口桩；已有项目降级为代码注释→工具生成API文档。

### 4.2 提交与PR强制绑定

- **提交规范强制关联**：Git提交信息必须标注影响的文档范围，示例：
  ```
  feat(backend): 新增用户登录接口
  docs-scope: docs/api/user.md, backend/user/DIR_META.md
  ```
- **PR门禁校验**：
  1. 代码变更PR必须包含对应文档变更，否则CI自动拦截，不予合入。
  2. 纯文档变更若涉及接口/逻辑契约，必须关联对应代码变更计划。
  3. 核心接口变更必须同时更新代码、API文档、前端类型定义。

### 4.3 自动化一致性校验

- 接口层：使用OpenAPI Generator，实现「文档生成代码桩」或「代码生成文档」，双向校验字段、类型、路径是否一致。
- 逻辑层：自定义CI脚本，校验核心函数的入参数量、返回值结构与文档描述是否匹配。
- 目录元数据：校验目录下文件增删后，对应`DIR_META.md`是否同步更新。

---

## 五、闭环交付 Loop 机制

核心目标：**主干分支永远可发布，每个开发单元都是完整可验证的闭环，禁止半吊子代码遗留**。

### 5.1 最小交付单元（MDU）

任何开发任务都必须拆分为「最小可交付单元」，满足：

- 具备完整的输入、处理、输出逻辑
- 有明确的验收标准，可独立测试验证
- 不依赖未完成的其他功能（可通过Mock解耦）
- 合入后不影响现有功能的正常运行

禁止拆分出「只写了一半接口」「页面搭了框架没逻辑」这类无法验证的任务。

### 5.2 迭代闭环门禁

每个MDU从开发到合入必须走完完整闭环（10关），缺一不可：

```
规格确认 → 代码开发 → 单元测试 → 文档同步 → 本地构建通过 → 测试环境部署 → 自动化测试 → 人工验收 → 合入主干 → 归档沉淀
```

任何一环不通过，都必须停在当前环节修复，不得流入下一阶段。

### 5.3 主干保护规则

- `main`/`trunk` 主干分支设为保护分支，禁止直接提交，只能通过PR合入。
- PR合入前必须通过：代码检查、单测、构建、文档一致性校验。
- 合入后自动触发测试环境部署，验证主干可用性。
- 每次合入都对应一个可追溯的版本，随时可以回滚。

---

## 六、错误沉淀与经验复用机制

建立「记录→分析→转化→校验」的闭环，把踩过的坑变成前置约束，从根源上减少重复错误。

### 6.1 错误分级记录

在仓库根目录维护 `ERROR.md`，按分类结构化记录每个问题：

```markdown
## 分类：接口联调/部署运维/编码逻辑/设计缺陷
### 问题ID：ERR-001
- 问题描述：前后端字段命名不一致导致联调阻塞
- 出现时间：2026-xx-xx
- 出现次数：3次
- 根因：接口契约未明确命名规范，双方各自命名
- 解决方案：统一采用驼峰命名，契约评审增加命名校验环节
- 沉淀产物：API_SPEC.md新增命名规范章节、CI增加字段命名校验脚本
```

### 6.2 根因分析与分级处理

- 偶发问题：记录解决方案，供后续查阅。
- 重复出现≥2次的问题：必须做5Why根因分析，找到流程/规范上的漏洞。
- 线上故障：必须输出故障复盘报告，更新对应模块的规格与Checklist。

### 6.3 经验转化为前置约束

把错误沉淀转化为三道防线：

1. **规范层**：更新Keel文档、开发规范，从规则上规避。
2. **工具层**：写入CI校验、代码检查规则，自动拦截。
3. **意识层**：每个迭代开发前，过一遍对应模块的「避坑Checklist」。

---

## 七、前后端 Git 管理与变更记录规范

推荐采用**单体仓库（Monorepo）** 模式，前后端代码放在同一仓库的不同目录，统一版本、统一文档、统一流程。

### 7.1 目录结构示例

```
project-root/
├── PROJECT_SPEC.md      # L0 项目总规格
├── ERROR.md             # 错误沉淀库
├── CHANGELOG.md         # 版本变更记录
├── docs/                # 全局文档
├── frontend/            # 前端代码
│   └── src/...
├── backend/             # 后端代码
│   └── src/...
└── deploy/              # 部署配置
```

### 7.2 分支模型（Trunk-Based 适配全栈开发）

- **`main`**：主干分支，始终可发布，受保护。
- **`feature/[模块]-[功能名]`**：特性分支，从main拉出，开发单个MDU，完成即删。
  - 示例：`feature/user-login`、`feature/backend-user-api`
- **`fix/[问题描述]`**：缺陷修复分支，从main拉出，修复完成合入即删。
  - 示例：`fix/login-token-expire`
- **`hotfix/[问题描述]`**：线上紧急修复分支，从main拉出，修复完即删。
  - 示例：`hotfix/payment-callback`
- **`release/vX.Y.Z`**（可选）：发布分支，用于版本发布准备，发布完即删。
- 禁止长期存在的开发分支，特性分支生命周期建议≤3天，最长不超过1周。

### 7.3 提交与变更记录规范

- 提交信息遵循 **Conventional Commits** 规范：
  ```
  &lt;type&gt;(&lt;scope&gt;): &lt;subject&gt;
  示例：
  feat(frontend): 新增登录页面表单
  fix(backend): 修复用户查询接口分页错误
  docs(api): 更新登录接口响应字段说明
  ```
- 每个版本发布自动生成 `CHANGELOG.md`，按版本分类记录feat、fix、docs、breaking change。
- 重大架构决策、技术选型变更，单独写入 `docs/adr/` 目录（架构决策记录），永久留存。

---

## 八、渐进式提示词加载与文件定位机制

专为AI辅助开发设计，实现「先读目录索引，再精准定位文件」，既节省上下文Token，又让AI明确修改边界。

### 8.1 目录元数据规范

每个层级目录下必须放置 `DIR_META.md`，作为该目录的索引说明书，格式固定：

```markdown
# 目录元数据：/backend/user
## 目录职责
用户模块核心业务逻辑，包含用户注册、登录、信息管理相关接口与服务。

## 子目录说明
- controller/：接口控制层，处理HTTP请求与响应
- service/：业务逻辑层，核心业务规则
- dao/：数据访问层，数据库操作
- entity/：数据实体定义

## 核心文件与可修改性
- user.controller.ts 【可修改】：用户接口控制器
- user.service.ts 【谨慎修改】：核心业务逻辑，修改需评估影响
- user.entity.ts 【禁止修改】：数据库实体映射，变更需走表结构评审

## 不变约束
- 所有接口必须做参数校验
- 密码必须加密存储，禁止明文返回
- 用户信息查询必须鉴权

## 最近变更
2026-07-14 新增手机号登录能力
```

### 8.2 AI渐进式加载流程

1. **第一层：全局认知**
   仅读取根目录 `PROJECT_SPEC.md` 和根目录 `DIR_META.md`，了解项目整体目标、技术栈、全局规则，不读取任何业务代码。

2. **第二层：模块定位**
   根据用户任务，定位到对应模块目录，读取该模块的 `MODULE_SPEC.md` 和 `DIR_META.md`，确认目标文件位置、可修改性、约束规则。

3. **第三层：精准读取**
   只读取任务涉及的目标文件内容，进行代码编写、修改、评审。

4. **第四层：同步更新**
   修改完成后，若影响了目录职责、文件列表、约束规则，必须同步更新对应层级的 `DIR_META.md`。

**知识金字塔（四层控制AI行为）**：
- **Prompt**：解决「这一次做什么」（单次对话）
- **AGENTS.md**：解决「在这个项目里怎么做」（工作区自动加载）
- **Rules**：解决「这类代码应该怎么写」（按文件范围生效的工程规范）
- **Skills**：解决「这类任务怎么跑流程」（按需触发的SOP）

**上下文三层治理**：
- 常驻上下文（Permanent）：项目地图、AGENTS.md、全局约束（始终加载）
- 按需上下文（On-Demand）：接口定义、模块规格、Rules（按任务动态加载）
- 任务级上下文（Task-Specific）：本次报错、临时目标（完成即丢弃，新任务新会话）

### 8.3 可修改性分级规则

- **【禁止修改】**：架构核心、实体定义、基础配置、第三方封装，修改必须走评审流程。
- **【谨慎修改】**：公共组件、工具函数、通用接口，修改需检查所有调用方，同步更新调用方。
- **【可修改】**：业务逻辑代码、页面组件、非核心配置，在规格范围内可直接修改。

---

## 九、补充规则：关键事项

### 9.1 环境与配置

- **环境一致性**：用Docker+Docker Compose统一开发、测试、生产环境，杜绝「本地能跑线上崩」。
- **配置分离**：不同环境的配置文件分离，敏感配置走环境变量，不提交到仓库。
- **依赖锁定**：前后端都锁定依赖版本（package-lock.json、go.mod等），禁止随意升级依赖。

### 9.2 数据与安全

- **数据库变更**：表结构修改必须写迁移脚本，纳入版本管理，禁止手动改库。
- **安全左移**：Keel阶段就明确鉴权、权限控制、输入校验、数据脱敏要求，开发时同步实现。
- **数据备份**：生产环境定时备份，上线前必须验证备份与回滚能力。

### 9.3 质量与可观测性

- **日志规范**：统一日志格式、级别，关键链路必须有TraceId，方便问题排查。
- **监控告警**：核心接口、关键指标必须配置监控告警，异常提前发现。
- **性能基线**：核心接口设定性能基线，每次迭代做性能回归测试。

### 9.4 版本与兼容

- **语义化版本**：遵循 `主版本.次版本.修订号`，主版本号变更代表不兼容升级。
- **向下兼容**：接口变更优先做兼容扩展，旧接口标记废弃，保留至少一个版本周期再删除。
- **回滚预案**：每个版本发布前，必须明确回滚步骤，确保出问题能5分钟内回退。

---

## 十、不同角色Keel实践要点

AI是加速器而非替代品，人机分工、上下文质量、模板驱动是落地关键。

### 产品经理（PM）
- AI是加速器而非替代品：业务理解、用户洞察、优先级判断仍由人把控
- 先梳理再生成：先整理关键需求、验收标准、业务约束，再交给AI生成PRD
- 分模块生成：复杂功能拆模块逐个生成，避免一次性输入过多
- PRD+Demo联动：评审会同时看文档与可交互Demo，提升对齐效率

### 前端开发
- AI定位从「终点」改成「加速初稿」：先生成可用初版，再做关键微调
- 模块化拆分而非整页一把梭：按组件/模块逐步生成，避免整页生成导致代码难维护
- 优先复用：明确告诉AI优先使用已有组件库，不要重复造轮子
- 多模态辅助：截图+设计稿辅助理解，减少布局误解

### 后端开发
- Code First：能让代码读懂的不要重复写文档（如类型定义、函数签名），避免过时成本
- 只补关键点+多做索引：业务知识重点写代码读不到的部分（业务规则、历史债、约定俗成）
- 渐进式加载上下文：避免全量上下文导致注意力稀释
- 模板驱动分段生成：SOP内聚在模板里，TRD按章节逐段产出由人监督

### 测试（QA）
- QA在决策位：策略/风险/异常判断由人，分析/生成/调试由AI
- 上下文要够全：PRD+技术方案+设计稿+评审纪要+已知细节
- Prompt工程化：角色+目标+技能+工作流+约束五段式
- 按需求规模分级保障：大/中/小型需求匹配不同测试深度

### 新人/新项目
- Spec Driven：有方案再实施，即便简单bugfix也先让AI出方案再执行
- 多给案例减少自由发挥：SDK/协议相关，把源码放到本地让AI检索
- 强Review防AI屎山：AI代码容易冗余/低质量，需及时纠偏
- 你必须懂如何实现：只有自己清楚才能发现AI漏洞

---

## 十一、落地工具链推荐

| 能力项 | 推荐工具 |
|--------|----------|
| Keel文档编写 | Markdown + VitePress/MkDocs（生成文档站） |
| 接口契约 | OpenAPI(Swagger) + Apifox/Postman |
| Git管理 | Git + GitHub/GitLab/Gitee |
| CI/CD校验 | GitHub Actions / GitLab CI |
| 代码质量 | ESLint / Pylint / SonarQube |
| 环境部署 | Docker + Docker Compose |
| 自动化测试 | Jest（前端）、Pytest（后端）、Newman（接口） |
| 错误沉淀 | 仓库内ERROR.md + 飞书/Notion（知识库） |
| 反馈闭环 | Lint/Test/Typecheck/Build + 日志监控（Harness Engineering） |
| Hooks自动化 | 生命周期Hooks（SessionStart/PreCompact/Stop/PreToolUse）+.sdd/运行时状态 |

---

## 附：模板文件索引

配套模板文件位于 `Keel-skill/templates/` 目录下：

- `PROJECT_SPEC.template.md` - L0 项目总纲模板
- `MODULE_SPEC.template.md` - L1 模块规格模板
- `API_SPEC.template.md` - 接口规格模板（OpenAPI格式）
- `DIR_META.template.md` - 目录元数据模板
- `ERROR.template.md` - 错误记录模板
- `CHANGELOG.template.md` - 变更日志模板
- `ADR.template.md` - 架构决策记录模板
